import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  callOutWrapper: {
    borderColor: '#94c600',
    borderRadius: 6,
    borderWidth: 3,
    margin: -9,
    padding: 6,
  },
});
