import { List } from 'immutable';

const initialState = new List;

export default function interfaceEditorInspectorStyleEditorInputBorderCornersInputSelectedCorners(state = initialState) {
  return state;
}
