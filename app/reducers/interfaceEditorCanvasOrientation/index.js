import { portraitOrientation } from '../../constants/canvasOrientations';

const initialState = portraitOrientation;

export default function interfaceEditorCanvasOrientation(state = initialState) {
  return state;
}
