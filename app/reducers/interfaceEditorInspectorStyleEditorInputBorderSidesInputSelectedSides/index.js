import { List } from 'immutable';

const initialState = new List;

export default function interfaceEditorInspectorStyleEditorInputBorderSidesInputSelectedSides(state = initialState) {
  return state;
}
