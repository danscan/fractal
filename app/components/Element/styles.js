import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  callOutWrapper: {
    borderRadius: 2,
    shadowColor: '#94c600',
    shadowRadius: 3,
    shadowOpacity: 2,
    shadowOffset: { height: 0 },
  },
});
