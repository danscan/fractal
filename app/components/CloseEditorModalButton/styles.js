import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    padding: 8,
  },

  closeButtonImage: {
    height: 14,
    width: 14,
  },
});
