import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },

  navigationBar: {
    height: 48,
  },

  scene: {
    backgroundColor: 'white',
    borderTopColor: '#ccc',
    borderTopWidth: 1,
    flex: 1,
  },
});
