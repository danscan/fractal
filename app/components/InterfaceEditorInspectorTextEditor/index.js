import React, { Component, View } from 'react-native';
import styles from './styles';

export default class InterfaceEditorInspectorTextEditor extends Component {
  render() {
    return (
      <View style={styles.container}/>
    );
  }
}
