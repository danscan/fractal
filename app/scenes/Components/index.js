import React, { Component, ScrollView, Text } from 'react-native';

export default class ComponentsScene extends Component {
  render() {
    return (
      <ScrollView>
        <Text>
          Components scene...
        </Text>
      </ScrollView>
    );
  }
}
